/**
 * @class JavaScript.Array
 * The constructor used for arrays.
 */
JavaScript.Array = Ext.extend({},{
  /**
   * The Array constructor used to initialize a new array object. A shortcut of <tt>[]</tt> will perform the same function.
   * @constructor
   * @param {Integer|*} element length of array or 0-n elements to initialize the array with
   */
  constructor: function(element){},
  /**
   * Property indicating the number of elements in the array.
   * @type Integer
   * @property length
   */
  length: 0,
  /**
   * Removes the last element on the array.
   * @return {*} Returns the element that was just removed.
   */
  pop: function(){
    return element;
  },
  /**
   * Appends elements to the end of the array.
   * @param {*} element The 1-n elements to append to the array.
   * @return {Integer} Returns the new length of the array after appending elements.
   */
  push: function(element){
    return this.length;
  },
  /**
   * Reverses all elements in the array. First becomes last, and vice versa.
   * @return {Array} Returns the reversed array.
   */
  reverse: function(){
    return elements;
  },
  shift: function(){},
  sort: function(){},
  splice: function(){},
  unshift: function(){},
  concat: function(){},
  join: function(){},
  slice: function(){},
  toSource: function(){},
  toString: function(){},
  indexOf: function(){},
  lastIndexOf: function(){},
  filter: function(){},
  forEach: function(){},
  every: function(){},
  map: function(){},
  some: function(){},
  reduce: function(){},
  reduceRight: function(){}
}); 